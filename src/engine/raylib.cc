#include "engine/raylib.h"

#include "engine/assets/texture.h"

namespace raylib {
  // void DrawTexture2(const engine::assets::Texture& tex, float x, float y, float scale) {
    // DrawTextureEx(tex, {x, y}, 0.f, scale, WHITE); 
  // }
} // namespace raylib